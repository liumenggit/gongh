export enum IntegralStartType {
    '可用' = -1,
    '失效的' = 0,
    '已完成' = 1,
}

export enum IntegralType {
    '签到' = 1,
    '核销' = 2,
    '后台' = 3,
    '积分抽奖' = 4,
}


export enum IntegralRecordTabs {
    '增加' = 1,
    '减少' = 2,
}
